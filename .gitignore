node_modules/
.env
uploads/
dist/

# Raw hero animation source frames (~97MB) — run `npm run optimize:hero-frames`
# in client/ to regenerate the compressed WebP set that actually ships.
client/src/assets/animation/
